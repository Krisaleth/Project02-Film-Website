@model IEnumerable<Project02.Models.Hall>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Capacity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cinema)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Capacity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cinema.Cinema_ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Hall_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Hall_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Hall_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
