@model IEnumerable<Project02.Models.Genre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Genre_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre_Slug)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Genre_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre_Slug)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Genre_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Genre_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Genre_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
